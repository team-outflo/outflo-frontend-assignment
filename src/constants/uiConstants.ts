// UI-related tweakable constants

// Maximum characters to display for lead name/url in the first column
export const LEAD_NAME_MAX_LENGTH = 40;

// Leads table minimum column widths (in pixels)
export const LEADS_TABLE_MIN_WIDTHS = {
  LEAD: 320,
  CONNECTION_STATUS: 180,
  ENGAGEMENT_STATUS: 200,
  LAST_ACTIVITY: 200,
  ASSIGNED_ACCOUNT: 220,
} as const;

